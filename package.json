{
  "name": "github-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker for GitHub API with token authentication",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"No tests specified\""
  },
  "dependencies": {
    "itty-router": "^3.0.13"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
